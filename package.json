{
  "name": "tile-realtime-timetable",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:client && npm run typecheck",
    "build:client": "tsc -p tsconfig.browser.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}

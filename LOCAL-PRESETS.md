# 개인 프리셋과 방과후

- 시간표 아래 **방과후 관리**에서 최대 20개 시간대를 추가하거나 개별 삭제합니다. 시작·종료 순서와 기존 일과와의 겹침을 검사합니다.
- 과목 칸을 누른 뒤 **이 칸 비우기**를 선택하면 해당 요일의 수업·메모·개별 교실/교사 정보만 비웁니다. 빈칸을 눌러 다시 추가할 수 있습니다. 비운 상태는 새로고침 뒤에도 유지됩니다.
- **도구 → 개인 프리셋**에서 현재 구성에 이름을 붙여 저장합니다. 학교·시간표·방과후·메모·개인 설정을 포함하며, Lightframe 인증 세션이나 비밀번호는 포함하지 않습니다.
- 프리셋은 현재 브라우저의 `tile-personal-presets-v1` 저장소에만 저장됩니다. 서버나 창작마당으로 전송되지 않습니다. 다른 기기와 자동 동기화되지 않으며 브라우저 데이터를 삭제하면 사라집니다. 기존 파일 내보내기와 계정 백업에는 현재 구성만 포함되고 프리셋 목록은 포함되지 않습니다.
- 불러오기는 현재 구성을 교체하므로 한 번 더 확인합니다. 적용 전 구성은 기존 **마지막 변경 되돌리기**로 복원할 수 있습니다.
- 시간대 삭제와 칸 비우기도 되돌릴 수 있습니다. 프리셋 목록에서 삭제한 항목은 직후 알림의 **되돌리기**로 복구합니다.
- 창작마당은 개발 중 표시만 제공하며, 이번 기능으로 공개 등록이나 배포를 하지 않습니다.

검증: `node --test tests/*.test.cjs`, `npm run build`. 테스트는 로직 수준이며 실제 브라우저 및 계정 왕복 검증을 대체하지 않습니다.
